<%if event.meetings.where(attending: true, voted: true).count >= 2 %>
  <%= link_to "Le récap'", event_path(event), class: "btn-blue-wm share btn-sticky", data: { event: params[:event_id] } %>
<% else %>
  <%= link_to "Fais voter tes amis", event_suggested_bars_path(event), class: "btn-blue-wm share btn-sticky", data: { event: event.id } %>
<% end %>
