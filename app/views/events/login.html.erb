<%= form_tag event_suggested_bars_path(@event) do %>
Qui es-tu ?

  <% @users.each do |user| %>
    <%= link_to user.name, event_path(@event, login_show: user.phone_number), class: "link-login" %>
  <% end %>
<% end %>
